/**
 * Pixel number for each breakpoint used in js for dynamic behaviors
 */
export enum NumericBreakpoint {
  Mobile = 480, // 30em
  Tablet = 839, // 52.4375em
  Desktop = 1024 // 64em
}

/**
 * Breakpoints used in the media-queries and the grid system
 */
enum Breakpoint {
  Mobile = '30em', // 480px
  Tablet = '52.4375em', // 839px
  Desktop = '64em' // 1024px
}

export default Breakpoint;
